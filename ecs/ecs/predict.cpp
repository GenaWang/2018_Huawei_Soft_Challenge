#include "predict.h"
#include <stdio.h>
#include<string>
using namespace std;

void predict_server(char * info, char * data ,char * filename)
{
	pre(info,data,filename);
	//const char * result_file = result.c_str();


	//write_result(result_file, filename);
}
